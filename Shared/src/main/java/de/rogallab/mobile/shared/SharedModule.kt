package de.rogallab.mobile.shared

/**
 * Marks the reusable Android Library module in the initial teaching example.
 *
 * Domain models, repositories, database infrastructure, network code, and
 * reusable Compose components can be moved into this module in later stages.
 * The app module depends on Shared, while Shared remains independent of app.
 */
object SharedModule
